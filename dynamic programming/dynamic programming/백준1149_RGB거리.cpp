#include <iostream>
#include <algorithm>
#include <vector>

using namespace std;
int red[3] = { 0, };
int green[3] = { 0, };
int blue[3] = { 0 };

int w(int r, int g, int b) {

}

int main() {
	int N;
	int r, g, b;

	for (int i = 0; i < N; i++) {
		cin >> r >> g >> b;
		w(r, g, b);
	}
}